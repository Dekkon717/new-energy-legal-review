---
version: 1.0
name: new-energy-legal-review
description: 面向新能源企业法务第一轮合同筛查的稳健、可复核工作台设计语言。
colors:
  paper: "#f4f7f4"
  card: "#ffffff"
  ink: "#17201f"
  muted: "#72807b"
  line: "#dfe7e2"
  teal: "#0f6e63"
  mint: "#d9f1e9"
  orange: "#da744e"
  red: "#bd564c"
typography:
  display: "Manrope, Noto Sans SC, sans-serif"
  body: "Noto Sans SC, Manrope, sans-serif"
  utility: "DM Mono, monospace"
rounded: "12px controls, 16px cards, 24px major surfaces"
spacing: "8px base rhythm"
components:
  primary_button: "{color.teal}"
  secondary_button: "{color.paper}"
  card_surface: "{color.card}"
  risk_redline: "{color.red}"
  risk_attention: "{color.orange}"
---

# DESIGN.md

## Visual Theme & Atmosphere

这是一个“安静但有判断力”的法务工作台。视觉来源不是泛化的科技面板，而是合同页、批注纸、项目清单和现场验收记录：纸张底色、细边界、克制的阴影、少量橙色提示线，以及可以被逐项复核的层级。

产品面对的是需要快速定位问题的法务、采购和项目人员。页面的单一任务是让用户从“上传一份合同”走到“知道下一步要核对什么”。所有装饰都要服务于这个路径。

## Color Palette & Roles

| Token | Hex | 用途 |
| --- | --- | --- |
| paper | `#f4f7f4` | 页面底色，像一张略带冷调的工作纸 |
| card | `#ffffff` | 内容表面和可读文本区域 |
| ink | `#17201f` | 标题、正文和高优先级信息 |
| muted | `#72807b` | 辅助说明、时间、来源类型 |
| line | `#dfe7e2` | 分隔线、卡片边界、输入框边界 |
| teal | `#0f6e63` | 主操作、已识别、积极状态 |
| mint | `#d9f1e9` | 本地处理、轻量提示、图标底 |
| orange | `#da744e` | 当前步骤、选中态、需要注意的事项 |
| red | `#bd564c` | 法律红线和明确错误，不用于普通警告 |

不要用渐变、荧光蓝、深色玻璃拟态或大面积彩色背景来制造“科技感”。风险颜色必须有语义：红色只说明需要优先人工确认，不代表系统已经作出最终法律判断。

## Typography

- 展示标题使用 Manrope 与 Noto Sans SC，字重 700–800，字距略紧；它负责建立产品个性，但不抢正文信息。
- 正文使用 Noto Sans SC，字号 12–15px，行高 1.7–1.9，确保长条款和说明可连续阅读。
- 数据、规则编号、版本号和来源类型使用 DM Mono，字号 10–12px；只用于真正的工具信息。
- 标题尽量使用短句和具体名词，不写“赋能、智能化、全链路”等无法帮助用户做决定的词。

## Layout

- 页面最大宽度 1480px，桌面左右内边距 34px；主要工作区使用双栏：左侧审查概览，右侧风险内容。
- 使用 8px 间距节奏。主要间距优先选择 8、16、24、32、48、64px，避免每个组件都出现新的数字。
- 首页首屏是两栏：左侧说明这套工具解决什么问题，右侧放文件入口；文件入口是主交互，不应被装饰性插画盖住。
- 工作区上方的标签表达真实的工作路径：合同审查、相似案例、法规依据。选中态用细橙线，不用大面积填充。
- 信息密度可以高，但要靠标题、细线、留白和风险分组分层；不要把每项内容都放进圆角卡片。

## Elevation & Depth

卡片使用 `#dfe7e2` 边界和非常轻的绿灰阴影。主表面最多有两级高度：页面底色与白色内容表面；风险分组可以用极浅的语义底色区分，但不建立多层漂浮面板。

## Shapes

- 主工作区和上传卡片：24px 圆角。
- 风险分组和内容卡片：15–16px 圆角。
- 控件：9–12px 圆角；状态标签可以使用胶囊形，但不要把所有文字都包成胶囊。
- 边框优先于阴影表达边界。按钮和输入框必须有清楚的 hover、focus、disabled 状态。

## Components

### 文件入口

文件入口是一个有明确边界的虚线区域，显示支持的格式、本地读取提示和当前文件名。拖入时边框变 teal、图标反色、文案告诉用户“松开鼠标，开始读取”。失败时说明发生了什么以及下一步，例如扫描件需要先 OCR。

### 风险卡片

风险卡片固定按“等级 / 优先级 / 置信度 → 标题 → 风险说明 → 条款要素 → 证据状态 → 命中条款 → 修改建议 → 谈判条款 → 人工复核”排列。不要把建议藏进 hover，也不要只用颜色表达等级。

### 按钮

- 主按钮：teal 背景、白色文字，只用于开始审查等主要动作。
- 次按钮：白底细边界，用于导出和辅助动作。
- 文字按钮：只用于低成本动作，如清除筛选、复制条款、查看案例库。
- 动词保持一致：按钮写“开始审查”，状态也写“已开始审查”；不要在不同位置换成“提交、运行、执行”。

### 空状态和错误

空状态应给出下一步动作，而不是只显示一个图标。错误要具体，避免“出了点问题”；同时保留用户已经选择的文件名和筛选条件，减少重做成本。

## Do's and Don'ts

- 做：用真实的新能源业务词汇、合同字段和法务工作流建立视觉个性。
- 做：每个风险项都提供可定位条款和人工复核入口。
- 做：保留可见的键盘焦点、足够对比度和清晰的禁用态。
- 不做：不使用通用 SaaS 仪表盘、无意义的渐变、过量圆角、装饰性机器人图标或空泛的 AI 宣传语。
- 不做：不把“规则命中”写成“法律结论”，不把候选案例数量写成已核验案例数量。
- 不做：不为了显得复杂而加入动画；动效只能帮助理解状态变化。

## Responsive Behavior

- 900px 以下，首页变为单栏，审查概览移到风险内容上方，案例卡片变为两列。
- 640px 以下，页面边距缩到 16px，案例和风险内容变为单列，标签栏允许横向滚动；关键操作仍保持可见。
- 所有可操作控件尽量达到 44px 触控高度；不能依赖 hover 才能发现功能。
- `:focus-visible` 使用清晰的 teal 外轮廓；系统设置减少动态效果时，关闭位移和过渡。
- 长文件名和长法源标题必须截断或换行，不能造成页面横向溢出。
